# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Datasets and training artifacts (large binaries, not versioned)
data/
runs/
*.pt
*.onnx
wandb/
# Exception: models/ holds the specific deployed weights (small, deliberate),
# decoupled from the full training-run bookkeeping under runs/.
!models/
!models/*.pt

# .NET build artifacts
frontend/bin/
frontend/obj/
